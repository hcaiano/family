---
description: 
globs: 
alwaysApply: true
---
Here's things you have to use
- pnpm
- use project id `ogizfcdgeqslkfjwzfdg` for supabase
- never use `@supabase/auth-helpers-nextjs` because it's deprecated. use `@supabase/ssr` instead
- shadcn
- use `pnpm dlx shadcn@latest add component-name` to install shadcn componnets 
- tailwind 4
- supabase
- nextjs 15
- use kebab case for components like component-kebab.tsx
- run `pnpm types:supabase` to update db types
- run the migrations using supabase mcp
- always use tanstack form for forms
- use zustand when needed
- use zod when needed
- add reusable components in the /components folder but add specific feature components inside the app/feature-name/components/ folder
- check the database schema quickly by going to `./lib/supabase/database.types.ts
